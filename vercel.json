{
  "installCommand": "if cd flutter; then git pull && cd .. ; else git clone --branch 3.27.0 --single-branch https://github.com/flutter/flutter.git; fi && ls && flutter/bin/flutter doctor && flutter/bin/flutter clean",
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
